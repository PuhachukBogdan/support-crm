/**
 * Feature 033 + 038: the one signed-request dialect this product speaks —
 * `t=<unix>,v1=<hex>` over `"<t>.<raw body>"`. Channels and staff provisioning share it.
 */
export * from './hmac-signature';
