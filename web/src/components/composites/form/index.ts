export { AppForm, type AppFormProps } from './form';
// Re-export the shadcn Form field primitives so screens import the form system from one place.
export {
  Form,
  FormItem,
  FormLabel,
  FormControl,
  FormDescription,
  FormMessage,
  FormField,
} from '@/components/ui/form';
