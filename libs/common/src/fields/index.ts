/**
 * Feature 037 (roadmap 4.15, W30): the closed ticket-field TYPE catalogue. Types are code; field
 * definitions, option sets, forms and values are per-account rows in chats_db.
 */
export * from './field-types';
