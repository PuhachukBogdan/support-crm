// @crm/common uses the shared backend Jest base (spec 003 adds unit tests here:
// the config loader / refuse-to-start guard). testMatch picks up src/**/*.spec.ts.
module.exports = require('../../jest.config.base.cjs');
