import { Channels } from '@/features/channels/channels';

/**
 * Channels (block W15, roadmap 6.8 minimum / subpoint 3.10) — the Admin Center's second real
 * section: mail addresses and the API-channel key.
 */
export default function AdminChannelsPage() {
  return <Channels />;
}
