import { Settings } from '@/features/settings/settings';

/**
 * Personal settings (block W18, subpoints 5.2 + 5.3 — roadmap 8.7/8.8 minimum): the shell with its
 * three categories, and the theme that follows the person.
 */
export default function SettingsPage() {
  return <Settings />;
}
