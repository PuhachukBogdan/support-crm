import { Vip } from '@/features/vip/vip';

/**
 * The VIP tab (block W17, subpoints 4.4 + 4.5 + 4.6 — roadmap 9.10/9.7 minimum): the AM's own
 * workspace — their portfolio, their players' tickets, and writing first by email.
 */
export default function VipPage() {
  return <Vip />;
}
