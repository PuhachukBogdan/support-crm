import { Access } from '@/features/access/access';

/**
 * ⭐⭐ Access management (block W28, roadmap 9.8 — R45): roles and permissions in ONE window.
 * The second real section of the Admin Center; the engine beneath it is feature 011 + 024.
 */
export default function AdminAccessPage() {
  return <Access />;
}
