import { MacrosAdmin } from '@/features/macros-admin/macros-admin';

/**
 * ⭐ W29 (R46, roadmap 14.4/4.6) — one screen, three tabs: macros WORK (the ~97 re-enter here),
 * automations and triggers say Coming Soon and pretend nothing.
 */
export default function AdminMacrosPage() {
  return <MacrosAdmin />;
}
