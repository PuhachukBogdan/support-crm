import { Analytics } from '@/features/analytics/analytics';

/**
 * Analytics (block W20, subpoints 6.2 + 6.3 + 6.4 — roadmap 11.1 minimum): live numbers and one
 * chart, read straight from the journal.
 */
export default function AnalyticsPage() {
  return <Analytics />;
}
